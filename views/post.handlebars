{{!-- Displays a single post and also the ability to add a comment --}}
{{!-- 
'post' OBJECT  
{
    id,
    title, (blogpost title)
    content, (blogpost content)
    user_id,
    user : {
        username (op's username)
        }
    comments: [
        {
            content (comment)
            user: {
                username (commenter's username)
            }
        },
        ...{},
    ]
}
--}}