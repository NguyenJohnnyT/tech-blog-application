{{!-- Displays a list of the user's blogposts --}}
{{!-- 
obj dbUserData
properties : username, blogs
blogs: an array of objects containing blogposts
blogpost objects: id, title, date
--}}
<h3>Blog posts by you ({{post.[0].username}}</h3>
{{#each post.[0].blogs as |blog|}}
{{>dash-details}}
{{/each}}